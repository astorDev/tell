# Tell - Make Util V2

[Makefile is awesome](./inspirations/makefile/article/index.md) for defining project-specific scripts. Make util makes those scripts runnable. It works fine. However, the UX of the `make` util is just too old. That's where `tell` comes in to address various make util issues. Specifically:

- [Utilizes Positional Arguments](./positional/README.md) for working directory and first rule variable.