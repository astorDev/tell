name: Tell.Cli NuGet
on:
  push:
    paths:
      - 'cli/**'
      - 'makefile/lib/**'
      - '.github/workflows/cli-nuget.yml'
  workflow_dispatch:

jobs:
  publish:
    name: Pack & Publish nuget
    runs-on: ubuntu-latest
    steps:
      - uses: astorDev/versy/calver/nuget@main
        with:
          project: cli/Tell.Cli
          nuget-api-key: ${{ secrets.NUGET_API_KEY }}