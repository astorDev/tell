# `tell` Positional Arguments

`tell` util allows to use positional arguments for two things:

- Working Directory. Instead of `-C` in `make`.
- First Appearing Variable. Instead of `ARG_NAME=` in `make`.